name: CI

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: macos-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install harness tools
        run: brew bundle

      - name: Run checks
        run: make check
